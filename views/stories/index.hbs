<h2 class="public-title">Recipes</h2>
<ul class="center">
    <li>
        <form action="/stories/search/:query" method="GET">
            <div class="input-search">
                <div>
                    <input type="search" name="query" placeholder="Search by Title or Keyword" minlength="3" size="70">
                </div>
                <button class="btn search-btn" type="submit">&nbsp;Search&nbsp;</button>
            </div>
        </form>
    </li>
</ul>
<ul class="center">
        <div class="sort">
                <form action="/stories/newest/" method="GET">
                    <button class="btn">Newest</button>
                </form>
                <form action="/stories/oldest/" method="GET">
                    <button class="btn" value="-1">Oldest</button>
                </form>
                <form action="/stories/likes/" method="GET">
                    <button input type="likes" value="likes" class="btn">Most Likes</button>
                </form>
        </div>
</ul>
<div class="row">
    {{#each stories}}
        <div class="s12 m4">
            <div class="card">
                <div class="card-image">
                    {{{editIcon user ../user _id}}}
                </div>
                <div class="card-content">
                    <div class="chip">
                        <img src="{{user.image}}">
                        <a href="/stories/user/{{user._id}}">{{user.displayName}}</a>
                    </div>
                    {{!-- QT: Insert right-aligned 'Country of Origin' or Image/Icon of Country's Flag here. --}}
                    <h5 class="recipe-title">{{title}}</h5>
                    {{!-- QT: TheSpiderLady: Insert image here, using <= max-width of the card? (used CSS, not Materialize) --}}
                    <p>{{stripTags (truncate body 400)}}</p>
                </div>
                <div class="card-action center-align">
                    <a href="/stories/{{_id}}" class="btn grey">View Full Recipe</a>
                </div>
            </div>
        </div>
    {{else}}
        <p>No stories to display.</p>
    {{/each}}
</div>